import { type NextPage } from "next";
import Head from "next/head";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faRightToBracket } from "@fortawesome/free-solid-svg-icons";

const Setting: NextPage = () => {
  return (
    <>
      <Head>
        <title>Group Listen</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className="flex min-h-screen flex-col items-center bg-slate-800 py-8 text-white">
        <div className="w-2/3 text-lg font-semibold">Account Setting</div>
        <div className="flex w-2/3 gap-2">
          <button className="flex w-32 flex-row items-center justify-between rounded-lg border-2 border-red-500 p-2 hover:bg-red-500">
            Log Out
            <FontAwesomeIcon icon={faRightToBracket} className="w-4" />
          </button>
          <button className="flex w-32 flex-row items-center justify-between rounded-lg bg-red-500 p-2 hover:opacity-50">
            Delete Account
          </button>
        </div>
      </main>
    </>
  );
};

export default Setting;
